with "aws";

project TwitStream is
    for Source_Dirs use (".");
    for Main use ("twitstream.adb");

    package Builder is
        for Default_Switches ("Ada") use ("-gnat05");
    end Builder;

end TwitStream;
